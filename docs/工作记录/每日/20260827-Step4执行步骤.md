# 2026-08-27 · Step 4 执行步骤（按顺序操作）

> 代码已实现：`models/rag_llama_baseline.py`（v1 支持度门控）  
> 关联：[20260827-Step4最小修改方案.md](./20260827-Step4最小修改方案.md) · [05-RAG与RAG-KG基线操作手册.md](../05-RAG与RAG-KG基线操作手册.md)

---

## 0. 跑前确认

- [ ] AutoDL 上 GPU 空闲（`nvidia-smi`）
- [ ] `models/user_config.py` 为 **`RAGModel`**（已是则跳过）
- [ ] 权重在：`Llama-3.1-8B-Instruct` + `all-MiniLM-L6-v2`
- [ ] `.env` 里 `SILICONFLOW_API_KEY` 可用
- [ ] **把本机改动的代码同步到 AutoDL**（`rag_llama_baseline.py` + `.env` 新增项）

---

## Step 1 · 切 200 子集

编辑项目根 `.env`：

```bash
# 注释全量
# DATASET_PATH=/root/autodl-tmp/20260820-crag/crag_task_1_and_2_dev_v4.jsonl.bz2

# 启用 200 子集（路径按你机器改：相对路径或绝对路径）
DATASET_PATH=example_data/dev_subset_200_typical_20260825_222321.jsonl.bz2
```

确认文件存在：

```bash
ls -lh example_data/dev_subset_200_typical_20260825_222321.jsonl.bz2
```

拒答开关先关（对照基线）：

```bash
RAG_ABSTAIN_ENABLED=0
RAG_ABSTAIN_MIN_MAX_SCORE=0.40
```

- [ ] Step 1 完成

---

## Step 2 · 子集 RAG 原版（对照）

```bash
cd /root/autodl-tmp/CRAG   # 按你的仓库路径
source .venv/bin/activate
unset OMP_NUM_THREADS

LOG=logs/eval_rag_subset200_baseline_$(date +%Y%m%d_%H%M%S).log
python local_evaluation_deepseek.py 2>&1 | tee "$LOG"
```

日志末尾抄四指标，填下表 **「RAG 原版」** 行：

| 运行 | RAG_ABSTAIN | τ | score | accuracy | hallucination | missing | 日志 |
|------|-------------|---|-------|----------|---------------|---------|------|
| RAG 原版 | 0 | — | | | | | |
| RAG+拒答 | 1 | 0.40 | | | | | |
| （可选调参） | 1 | 0.35 / 0.45 | | | | | |

- [ ] Step 2 完成

---

## Step 3 · 子集 RAG + 拒答

编辑 `.env`：

```bash
RAG_ABSTAIN_ENABLED=1
RAG_ABSTAIN_MIN_MAX_SCORE=0.40
```

再跑（建议新日志名）：

```bash
LOG=logs/eval_rag_subset200_abstain_t040_$(date +%Y%m%d_%H%M%S).log
python local_evaluation_deepseek.py 2>&1 | tee "$LOG"
```

- [ ] Step 3 完成

---

## Step 4 · （可选）调阈值 τ

只改 `.env` 的 `RAG_ABSTAIN_MIN_MAX_SCORE`，分别试：

```text
0.30   0.35   0.40   0.45
```

每次 `RAG_ABSTAIN_ENABLED=1` 重跑 Step 3，选 **hallucination↓ 且 score↑**（missing 略升可接受）的一组。

- [ ] Step 4 调参完成（或跳过，先用 0.40）

---

## Step 5 · 写结论

对照 Step 2 vs Step 3（最好的一组 τ）：

| 现象 | 含义 | 下一步 |
|------|------|--------|
| hallucination↓ 且 score↑ | 有效 | Day11 消融；可选全量 |
| 几乎不动 | 阈值弱或信号不对 | 调 τ 或规划 v2（一致性） |
| missing 暴涨、score 掉 | 拒答过狠 | 提高 τ（如 0.45） |

从 `api_responses/` 或日志里挑 **5 条变好 + 5 条变坏** case，简要记 query / 预测 / 原因。

在本文件或 `08-Step4后续工作细化.md` §6 勾选清单打勾。

- [ ] Step 5 结论已写

---

## tmux 长跑（推荐）

```bash
tmux new -s step4
source .venv/bin/activate && unset OMP_NUM_THREADS
python local_evaluation_deepseek.py 2>&1 | tee logs/eval_rag_subset200_xxx.log
# Ctrl+B D  detach
```

---

## 环境变量速查

| 变量 | 含义 | Step 2 | Step 3 |
|------|------|--------|--------|
| `DATASET_PATH` | 200 子集路径 | ✓ | ✓ |
| `RAG_ABSTAIN_ENABLED` | 0=原版 / 1=拒答 | **0** | **1** |
| `RAG_ABSTAIN_MIN_MAX_SCORE` | max 余弦分阈值 | 任意 | **0.40**（可调） |
| `RAG_ABSTAIN_RESPONSE` | 拒答文案（默认 `I don't know`） | 默认 | 默认 |

---

## 今日不必做

- 全量 2706 重跑
- Vanilla 子集（Step 4 重点是 RAG vs RAG+拒答）
- RAG-KG / 换模型

---

## 勾选总览

- [ ] Step 1 切子集 + `RAG_ABSTAIN_ENABLED=0`
- [ ] Step 2 子集 RAG 原版四指标
- [ ] Step 3 子集 RAG+拒答（τ=0.40）
- [ ] Step 4 可选调 τ
- [ ] Step 5 对比表 + 10 case + 结论
