# 2026-08-28 · Step4 结果分析：阈值为何压不动 + 下一步工作步骤

> 来源：Cursor 讨论整理（工作记录.md 当日条目）  
> 关联：[20260827-Step4执行步骤.md](./20260827-Step4执行步骤.md) · [20260827-Step4最小修改方案.md](./20260827-Step4最小修改方案.md) · [错误分析-200.md](../错误分析-200.md)

---

## 1. 最近有效跑次（200 子集）

| 运行 | τ | score | accuracy | hallucination | missing | 日志 |
|------|---|-------|----------|---------------|---------|------|
| RAG 原版 | — | **−0.30** | 24.5%（49） | 54.5%（109） | **21.0%（42）** | `eval_rag_subset200_baseline_20260827_235441.log` |
| RAG+拒答 | 0.40 | **−0.30** | 24.5%（49） | 54.5%（109） | **21.0%（42）** | `…abstain_t040_20260828_002518.log` |
| RAG+拒答 | 0.30 | −0.29 | 25.0%（50） | 54.0%（108） | **21.0%（42）** | `…abstain_t030_20260828_003532.log` |

**无效跑（勿计入）：** `…000858.log`（dotenv 修复前，与 baseline 全同）；`…002052.log`（中途停）。

### Step 5 结论（v1）

> **几乎不动。** τ=0.30 / 0.40 未抬高 missing，四指标与 baseline 等价（τ=0.30 的 ±1 为采样噪声）。  
> **不是「拒答方向错了」**，而是 **「只用 max 检索余弦分」在 CRAG 噪声网页上区分力不够。**

---

## 2. 为什么阈值压不动？（原因分析）

### 2.1 直接证据：门控几乎没触发

v1 规则：`max(cosine_scores) < τ` → 强制 `I don't know`。

若规则生效，**missing 必须上升**（强制拒答会计入 missing）。  
三次有效跑 **`n_miss` 全是 42** → 几乎没有样本被规则拦下。

τ=0.30 相对 baseline：正确 +1、幻觉 −1、missing 不变 → 符合 `temperature=0.1` 的波动，**不是拒答收益**。

### 2.2 机制层面：测错了「证据不足」的代理量

| 真实需要 | v1 实际用的信号 |
|----------|-----------------|
| 「这些片段是否足以支撑正确答案？」 | 「问题与 top 句的向量相似度高不高？」 |

相似度高 ≠ 证据充分：

- HTML 切句后，同主题句子很容易和 query 很像（实体名、题型词重复）→ **max 分偏高**；
- 高分片段仍可含噪声、过时信息、错列表项 → 模型照样幻觉（Phase B：**过度检索 / rag_worse**）；
- 「缺证据」类也可能仍有若干相关句，max 分不一定掉到 0.30 以下。

因此 τ=0.40、甚至 0.30，对多数题都「过得了门」——**阈值不是太紧，而是信号本身与失败模式不对齐**。

### 2.3 与错误分析早已对齐（方案文档已预警）

`20260827-Step4最小修改方案.md` §5：

> 过度检索 25 条全来自 `rag_worse_hallucination`——检索分数**不低**，但片段**误导**。**max 阈值压不住。**

Phase B Top-3：生成错 / **过度检索** / 缺证据。  
v1 最多指望压「max 分极低」的缺证据；子集上这类占比不足以拉动四指标，且实测几乎触发不到。

### 2.4 工程因素（已排除为主因）

| 因素 | 状态 |
|------|------|
| `.env` 在 `import UserModel` 之前未加载 | 已修；`…002518` / `…003532` 为修复后跑次 |
| 仍完全同 baseline（0.40） | 更支持「阈值几乎不触发」而非「开关仍坏」 |
| 裁判 / 数据不一致 | 同 200 子集、同 DeepSeek、同 RAGModel |

**待补强（下一步诊断）：** 显式落盘每题 `max_score`，确认 `<0.40` / `<0.30` 的条数是否接近 0。

### 2.5 小结（可写进预实验报告）

```
阈值压不动
  = 门控几乎不触发（missing 不变）
  + max 余弦分 ≠ 证据充分性
  + 主失败模式是「高分噪声 / 生成不忠于证据」，不是「检索分极低」
```

继续盲扫 τ=0.35/0.45 **期望收益极低**；把 τ 抬到 0.55+ 可能误杀正确样本，且未验证分布前不建议。

---

## 3. 下一步工作步骤（执行进度）

### 执行进度（2026-08-28）

| 步骤 | 状态 | 说明 |
|------|------|------|
| **A** max_score 诊断 | 🔄 脚本已写，待 AutoDL 跑 | `scripts/diagnose_retrieval_scores.py` |
| **B** 固化 v1 负结果 | ✅ | 见 §1；结论 **几乎不动** |
| **C1** 答案–片段一致性 | 🔄 代码已合入，待子集评测 | `RAG_ABSTAIN_V2_*` |

**本机无法跑 A/C1 评测**：无 MiniLM 权重；SSH AutoDL 连接拒绝。请开机同步后按下方命令执行。

---

### Step A · 诊断 max_score 分布（优先，半天内）

**目标：** 坐实「τ=0.30/0.40 几乎触发不到」。

| # | 动作 | 产出 |
|---|------|------|
| A.1 | 写/跑脚本：200 子集只做切块 + MiniLM 打分，**不调用生成 LLM、不调用裁判** | 每题 `interaction_id, max_score, mean_top5, top1-top2` |
| A.2 | 统计全体 / 幻觉 vs 正确 的分位数 | p10 / p25 / 中位数表 |
| A.3 | 计数 `max_score < 0.30 / 0.40 / 0.50 / 0.60` | 一张小表 |

**AutoDL 命令：**

```bash
cd /root/autodl-tmp/CRAG
# 同步本机：scripts/diagnose_retrieval_scores.py、rag_llama_baseline.py、.env、run_step4_subset200.sh
source .venv/bin/activate
bash scripts/run_step4_subset200.sh diagnose
# 或：python scripts/diagnose_retrieval_scores.py
```

看 `example_data/step4_max_score_diag_*.summary.json` 里的 `triggers_at_tau`。

- [x] A.1 脚本可跑（已写入仓库）
- [ ] A.2–A.3 分布表落盘（待 AutoDL）

---

### Step B · 写清 v1 负结果（✅）

结论：**几乎不动**（§1 三行有效数字）。不必再扫 τ=0.35/0.45。

- [x] B 结论已写入本文

---

### Step C · 实现 v2 最小改进（主战场）

| 优先级 | 机制 | 环境变量 |
|--------|------|----------|
| **C1** | 生成后：答案与 top-k 片段 max 余弦；过低 → `I don't know` | `RAG_ABSTAIN_V2_ENABLED=1`、`RAG_ABSTAIN_ANSWER_MIN_SIM=0.35` |

**代码：** `models/rag_llama_baseline.py`（`_abstain_config` + 生成后一致性检查；运行时读 env，避免 dotenv 时序问题）。

**AutoDL 对照（复用已有 baseline，不必重跑）：**

```bash
# 仅开 v2（关 v1）
bash scripts/run_step4_subset200.sh abstain_v2 0.35
```

判读：相对 baseline（score −0.30 / hall 54.5% / miss 21%）：

| 现象 | 下一步 |
|------|--------|
| hallucination↓ 且 score↑ | Day11 消融 |
| 几乎不动 | 调 sim 或试 C2（top1−top2 gap） |
| missing 暴涨 | 提高 `RAG_ABSTAIN_ANSWER_MIN_SIM`（如 0.25 更松？注意：sim 越高越严） |

注意：`ANSWER_MIN_SIM` **越高越容易拒答**（要求答案更贴近片段）。

- [x] C1 实现 + 可开关
- [ ] C1 子集对照四指标
- [ ] C1 结论

---

### Step D · 按 C1 结果分流

| C1 结果 | 下一步 |
|---------|--------|
| 有效 | Day11：消融（去掉一致性 / 只留 v1）+ 10 case；可选全量复核 |
| 几乎不动 | 试 C2；或检索侧降噪（少召回噪声句） |
| 拒答过狠 | 放宽一致性阈值；仅对非 missing 预测做二次检查 |

---

### Step E · 明确不做（本阶段）

- [ ] ~~再扫 τ=0.35 / 0.45 全量裁判~~
- [ ] ~~未经验证就全量 2706~~
- [ ] ~~换更大生成模型 / RAGFlow / RAG-KG~~
- [ ] ~~改 CRAG 评分公式~~

---

## 4. 建议时间线（2–4 天）

```
今天–明天
  → Step A 分数分布诊断
  → Step B 固化 v1「几乎不动」结论
之后 1–2 天
  → Step C1 答案–片段一致性拒答 + 子集对照
有信号
  → Step D → Day11
无信号
  → C2 或检索侧改法；Day12 去留
```

---

## 5. 一句话

**阈值压不动，是因为「问题–片段相似度」在噪声 HTML 上普遍偏高，拦不住「高分误导仍硬答」；下一步先量化 max_score 分布，再改用「答案–证据一致性」做 v2 拒答。**
