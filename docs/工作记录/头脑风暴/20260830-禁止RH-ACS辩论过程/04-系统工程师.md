# 04 · 系统工程师（禁止 RH-ACS 轮）

**任务：** 在 8B / MiniLM / 无任意 Web / 现有 `rag_llama_baseline.py` 栈上，判一周可验性；给 Go / 险 / No-Go。

---

## 1. 约束复述

- 不新训大模型；不依赖稳定外网 Web 纠检索  
- 200 子集能出四指标；改动集中在检索后处理 / prompt / 轻量规则  

---

## 2. 可行性表

| 点子 | 一周？ | 风险 | 备注 |
|------|--------|------|------|
| N1 AFFR | **Go** | 形态分类错 → 整题走错算子 | 规则+短判即可 MVP |
| N2 CBSD | **Go 最快** | \(k\) 估错会截断真答案 | set 子集先开；启发式估 k |
| N3 DSM | **Go** | 实体对齐漏召回 → missing↑ | near-dup 要调 |
| N4 TAR | **Go** | 日期解析失败则空转 | 无时间锚的题跳过 |
| N5 SKV | **Go** | 抽取质量差 → 全面变差 | **必须**原文 vs 表消融 |
| ζ 硬负重排 | Go | 增益可能小 | 插件 |
| ι 对比解码 | **险** | 改生成循环、×2 前向 | 本轮已否决，省事 |
| γ 证伪通行 | 紧 | 每候选再检索 | 二期 |

---

## 3. 建议落地顺序（若用户选题后）

```text
D1–2  N2：估 k + 截断/约束解码；set 开关
D3    smoke + set 切片表
D4–5  可选挂 N3 双源准入
D6–7  四指标 + set 幻觉对照；再决定是否开 N1/N4/N5
```

**工程成功线：** 有开关、有对照表、有 set 切片。  
**方法成功线：** set 幻觉↓ 且全子集 accuracy 不塌、score 不降。

---

## 4. 对 08 RH-ACS+ 代码

本轮**不实现、不合并**进同一开关树，避免又做成「伤害门 + 小插件」。双轨代码分支待选题会。
