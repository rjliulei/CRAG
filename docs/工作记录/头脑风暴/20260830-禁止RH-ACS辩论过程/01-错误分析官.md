# 01 · 错误分析官（禁止 RH-ACS 轮）

**任务：** 只基于本地桶；**禁止**谈闭卷回退 / a0 / 伤害门；换刀指出失败模式；给新信号方向。

---

## 0. 明确不谈什么

- rag_worse「如何回退闭卷」不当本轮主问题  
- 不提约束覆盖 c、不提 Astute 差界  

---

## 1. 换刀后的本地现象

| 现象 | 证据 | 研究含义（新刀） |
|------|------|------------------|
| set 幻觉重 | 29 条中 hallucination **21** | **名单膨胀 / 乱并**，不是「该不该检索」 |
| Pixar 例 | 多列 Toy Story 4 等，GT 不含 | **基数失控 + 成员准入无门** |
| simple 幻觉最多 | 50 条中 hallucination **29** | **证据不够仍吐完整实体** → 假填充 |
| 生成错 + 缺证据 | Top 错误标签 | **答的形态与证据形态错位**；HTML 噪声助攻 |
| 多片段 | 成员列表过长/含错人 | **不同句贡献人名 → 模型做并集** |

---

## 2. 本轮反对的假创新（加禁区）

- 一切 RH-ACS / 闭卷通道选择包装  
- v1 / v2 / 只调 top-k  
- 「整题充分性分数」换皮  

---

## 3. 支持的新假设（按贴桶程度）

1. **集合基数约束（后称 N2）** — 最贴 Pixar；信号是 \(k\)，不是相关分  
2. **双源成员准入（N3）** — 打拼名单；项级丢弃，非整题拒  
3. **答形态先判（N1）** — 打 simple/set 形态错配  
4. **时间锚下推（N4）** — 打带年份噪声页  
5. **KV 中间表（N5）** — 打 HTML→胡编  

---

## 4. Top-1

**N2 Cardinality-Bounded Set Decoding**（可与 N3 组合，但单点先做 N2）。
