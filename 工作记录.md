# 工作记录

这个项目用到rag了么？和ragflow的关系

Mock API 在这里的作用是什么？模拟？模拟那不成假数据了么？

写入操作手册。

不要修改工作记录，写入规则。

还有哪些前置操作，比如我要注册huggingface账号么？

我们的目标是什么？咱是在做调研确定研究方向吧？

所以呢？那我省点钱，换哪个？

保留原来代码，修改一版代码，支持deepseek-ai/DeepSeek-V3.2。
| **OpenAI API Key** | 有额度、能调用 GPT | `local_evaluation.py` 默认用 GPT-4 打分 |


更换gpt为哪个模型，效果最好？

换成其他的影响会很大么？对我的研究有什么影响？

提交并push

remote ssh插件，配置链接远程服务器

ssh -p 17947 root@connect.nmb1.seetacloud.com

（密码仅本地登录用，勿写入仓库）

将链接操作写入操作手册

配置对么

可以改变market的源么
原来装过ssh，现在应该是有残留，导致换源后，新安装的ssh没有界面。
卸载ssh。
还是报错。
写入操作文档。
meta-llama/Meta-Llama-3-8B-Instruct这个模型被拒了，我可以使用其他模型么？
meta-llama/Llama-3.1-8B-Instruct换成这个模型可以么？

## 2026-08-21
下载导师群里的文件。
跑通所有基线，制作100条测试集，快速验证方向是否可行。

### Vanilla 全量评测结果（`logs/eval_deepseek_20260821_225909.log`）

**结论：** Vanilla Llama-3.1-8B 全量跑通，综合分约 0（−0.0018），主因是大量拒答（missing ≈ 68%），答对与幻觉几乎打平。

| 项 | 内容 |
|---|---|
| 生成模型 | `Llama-3.1-8B-Instruct`（vLLM，~15GB，fp16） |
| 基线 | Vanilla（闭卷，不用检索） |
| 裁判 | 硅基流动 `deepseek-ai/DeepSeek-V3.2` |
| 样本数 | 2706 |
| 生成耗时 | ~8 分钟（约 677 batch） |
| 评测耗时 | ~74 分钟 |

| 指标 | 数值 | 条数 |
|---|---|---|
| **score** | **−0.00185 ≈ 0** | — |
| accuracy | 15.8% | 427 正确 |
| hallucination | 16.0% | 432 幻觉 |
| missing | 68.3% | 1847 不知 |

评分公式：`score = (2×correct + miss) / n − 1`（与日志一致）。

**解读：**

1. 综合分≈0：正确与幻觉几乎各半（427 vs 432），奖惩抵消；大量 `I don't know` 不奖不罚，把总分钉在 0 附近。
2. 真正作答时约五五开：859 条非拒答里正确率约 49.7%——闭卷碰上强依赖外部知识的题，符合预期。
3. 高 missing 是 Vanilla 的保守策略（系统提示鼓励不确定就说不知道），避免更差负分，也说明多数题答不上。
4. 流程正常；日志中的 debugpy / dtype 告警不影响结果。

**下一步建议：** 见下方 **2026-08-22**（决定全量跑 RAG）。

## 2026-08-22

### 接下来的工作：全量跑 RAG

**目标：** 与 Vanilla 同数据（2706）、同裁判（DeepSeek-V3.2），跑通网页 RAG 全量基线，对比四指标决定后续方向。  
**操作手册：** `docs/工作记录/05-RAG与RAG-KG基线操作手册.md` §2。

#### 清单

1. **确认 GPU 空闲**  
   `nvidia-smi`；停掉仍占显存的 Vanilla / 其它评测（`tmux ls` → 按需 `kill-session`）。

2. **下载句向量模型（若尚未有）**  
   `sentence-transformers/all-MiniLM-L6-v2` → `models/sentence-transformers/all-MiniLM-L6-v2/`。

3. **`.env` 切到全量数据**（与 Vanilla 那次一致，约 2706 条）  
   注释掉 `dev_subset_100_*`，启用全量路径，例如：  
   `DATASET_PATH=/root/autodl-tmp/20260820-crag/crag_task_1_and_2_dev_v4.jsonl.bz2`  
   （以服务器上实际文件为准；须与上次 Vanilla 同源，对比才有意义。）

4. **切换基线** — 编辑 `models/user_config.py`：  
   注释 `InstructModel`，启用 `RAGModel`（不启 Mock API）。

5. **tmux 全量评测**  
   ```bash
   cd /root/autodl-tmp/CRAG
   source .venv/bin/activate
   unset OMP_NUM_THREADS
   mkdir -p logs
   python local_evaluation_deepseek.py 2>&1 | tee logs/eval_rag_$(date +%Y%m%d_%H%M%S).log
   ```  
   预期：生成阶段慢于 Vanilla（HTML + 检索）；评测仍约 1 小时量级（看裁判 API）。  
   若 CUDA OOM：先把 `VLLM_GPU_MEMORY_UTILIZATION` 降到 `0.70`～`0.75`，或 `SUBMISSION_BATCH_SIZE=1/2`。

6. **记录结果并对比 Vanilla**  
   从日志末尾抄 `score` / `accuracy` / `hallucination` / `missing`，与 `eval_deepseek_20260821_225909.log` 并排写进本记录。

7. **（可选，RAG 之后）** RAG-KG：启 Mock API → 切 `RAG_KG_Model` → 可先用典型子集再决定是否全量。

8. **制作 100–200 条典型子集**（全量 RAG 对比完成后立刻做）  
   - **目的：** 后续改方法时用「快迭代集」，不再每次烧全量 2706。  
   - **怎么抽「典型」**（不要纯随机凑数）：在 Vanilla vs RAG 结果上按标签分层，例如  
     - `question_type`：simple / comparison / multi-hop / false_premise 等各留若干  
     - `domain`：五领域尽量覆盖  
     - 结果类型：correct / hallucination / missing 三类都要有（尤其 **RAG 仍错、Vanilla 对、或两者都 hallucinate** 的硬例）  
   - **落盘：** 如 `example_data/dev_subset_150_typical.jsonl.bz2`，`.env` 可切换；改算法时默认跑该子集。  
   - **用法：** 新想法先在子集上看四指标 + 抽查 case；有效再全量确认。

#### 判断标准（跑完看）

| 现象 | 含义 |
|---|---|
| RAG 的 score / accuracy 明显高于 Vanilla | 检索有效 → 后续可做切分、召回、重排、拒答策略 |
| 几乎不动或更差 | 先查检索是否生效、OOM、prompt / missing 是否飙升，再谈改方法 |

### 研究方向（调研阶段暂定）

**一句话：** 在 CRAG 上做 **「嘈杂网页检索下的可信问答」**——提高答对率的同时压住幻觉，并学会该拒答时拒答（score 友好的 missing）。

依据（当前证据）：

- Vanilla 全量：score≈0，missing≈68%，真正作答时对错≈五五开 → 瓶颈在 **外部知识 + 何时敢答**，不是「把 LLM 换成更大」 alone。
- CRAG 设计本身就是：多领域、多题型、时效性、长尾实体、网页噪声 → 适合研究 **RAG 可靠性**，而不是普通阅读理解刷榜。

**建议主线（选 1 条做深，其余当对照）：**

| 优先级 | 方向 | 具体可做 | 子集上怎么验 |
|---|---|---|---|
| ★ 主推 | **检索–生成–拒答 联合策略** | 证据不足则 `I don't know`；有证据再短答；降低「半对半幻觉」 | hallucination↓ 且 score↑（允许 missing 略升） |
| 备选 A | **噪声网页检索质量** | 更好切分 / 召回 top-k / 重排 / 去无关 HTML | 硬例从错→对，尤其 multi-hop / 长尾 |
| 备选 B | **题型专项** | 只啃 multi-hop 或 false_premise / 时效题 | 该题型 accuracy 拉开基线 |
| 暂缓 | 纯换更大生成模型、或上 RAGFlow 工程堆料 | 成本高、难写清学术贡献 | — |

**和 RAGFlow 的关系：** 本仓库是 **评测基准 + 基线**；RAGFlow 是工程产品。调研阶段用本仓库量效果；若以后做系统演示再考虑对接，不作为开题主贡献。

**定题节奏：**

1. 全量 Vanilla（已完成）→ 全量 RAG（进行中）→ 典型子集 100–200  
2. 在子集上做 error analysis（按题型/领域/对错类型归类）  
3. 选定上表主线中的一条，写清「基线弱点 → 方法假设 → 指标预期」  
4. 子集迭代通过后再全量复核  

> 主线可在 RAG 全量数字出来后微调：若检索几乎无效，先偏向备选 A；若检索已抬分但幻觉仍高，则主推「拒答/可信」更顺。

## 2026-08-20

### 文档

- 将仓库主要说明改为中英双语：`README.md`、`CONTRIBUTING.md`、`CODE_OF_CONDUCT.md`，以及 `docs/`、`mock_api/`、`models/`、`tokenizer/` 相关 README。
- 新增 `docs/工作记录/` 操作文档（面向本机/AutoDL 落地）：
  - `00-目录.md`：阅读索引
  - `01-硬件要求.md`：官方机型与选型说明
  - `02-通用运行步骤.md`：不绑平台的通用流程
  - `03-AutoDL单卡24GB操作手册.md`：主操作手册（单卡、改并行度、依赖与评估）
  - `04-项目代码解析.md`：仓库结构、端到端流水线、三基线原理与读码顺序
- 补充 `03-AutoDL单卡24GB操作手册.md` 步骤 B：AutoDL 学术加速（`source /etc/network_turbo`）、pip 国内镜像、HF 镜像 `HF_ENDPOINT`。

### 代码

- 调整 `local_evaluation.py`：
  - 兼容 ground truth 为字符串或列表
  - 去掉未使用的 `auto_gptq` 导入与多余的 OpenAI 客户端创建

### 备注

- 运行计划：AutoDL 单卡 24GB（推荐 RTX 4090），基线将 `VLLM_TENSOR_PARALLEL_SIZE` 设为 1。
- 大数据分片（如 Task3 压缩包）勿误提交进 Git。

